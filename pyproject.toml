[project]
name = "fdrsp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest-xdist>=3.8.0",
]

[dependency-groups]
dev = [
    "pandas>=3.0.2",
    "pytest>=9.0.3",
    "pytest-xdist>=3.8.0",
]
